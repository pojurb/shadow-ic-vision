@AGENTS.md

# Claude Code Adapter

`AGENTS.md` is the canonical shared playbook. This file exists only so Claude
Code loads that playbook automatically.

- Read the relevant policy module under `.agents/` before acting.
- Keep Claude-specific settings or loader notes here; do not duplicate shared
  lifecycle, quality, security, or release rules.
- If a shared rule changes, update the canonical file and its owning module, not
  a second copy here.
